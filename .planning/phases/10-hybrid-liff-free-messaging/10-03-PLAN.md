---
phase: 10-hybrid-liff-free-messaging
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - apps/web/app/(liff)/events/[id]/page.tsx
  - apps/web/app/(liff)/events/create/page.tsx
  - apps/web/app/(liff)/events/templates/[id]/edit/page.tsx
  - apps/web/lib/liff-messaging.ts
autonomous: false
requirements:
  - SC-2
  - SC-3
user_setup:
  - service: line-developers-console
    why: "sendMessages requires chat_message.write scope on LIFF app"
    dashboard_config:
      - task: "Enable chat_message.write scope on the LIFF app"
        location: "LINE Developers Console -> Your Provider -> LIFF channel -> LIFF tab -> Your LIFF app -> Scopes -> Add chat_message.write"

must_haves:
  truths:
    - "Register action inside LINE calls liff.sendMessages() with the flexCard from API response"
    - "Cancel action inside LINE calls liff.sendMessages() with the flexCard from API response"
    - "Event creation inside LINE calls liff.sendMessages() with the flexCard from API response"
    - "External browser actions send X-Liff-Context: external header so server pushes to group"
    - "sendMessages failure is silent (console.error only, no user-facing error)"
  artifacts:
    - path: "apps/web/app/(liff)/events/[id]/page.tsx"
      provides: "Register/cancel with sendMessages integration"
      contains: "sendMessages"
    - path: "apps/web/app/(liff)/events/create/page.tsx"
      provides: "Event creation with sendMessages integration"
      contains: "sendMessages"
  key_links:
    - from: "apps/web/app/(liff)/events/[id]/page.tsx"
      to: "liff.sendMessages"
      via: "useLiff hook"
      pattern: "liff.*sendMessages"
    - from: "apps/web/app/(liff)/events/[id]/page.tsx"
      to: "/api/proxy"
      via: "fetch with X-Liff-Context header"
      pattern: "x-liff-context"
---

<objective>
Wire client-side liff.sendMessages() into all user-initiated actions (register, cancel, create event) so Flex cards are sent for free inside LINE. External browser users send a header so the server falls back to pushMessage.

Purpose: Per D-04/D-03/D-06 — free messaging for in-LINE actions, paid fallback only for external browser.
Output: All user actions send Flex cards via sendMessages when inside LINE.
</objective>

<execution_context>
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-liff-free-messaging/10-CONTEXT.md
@.planning/phases/10-hybrid-liff-free-messaging/10-RESEARCH.md
@.planning/phases/10-hybrid-liff-free-messaging/10-01-SUMMARY.md
@.planning/phases/10-hybrid-liff-free-messaging/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sendMessages to register/cancel actions on event page</name>
  <files>apps/web/app/(liff)/events/[id]/page.tsx, apps/web/lib/liff-messaging.ts</files>
  <read_first>
    - apps/web/app/(liff)/events/[id]/page.tsx
    - apps/web/components/liff/liff-provider.tsx
    - .planning/phases/10-hybrid-liff-free-messaging/10-RESEARCH.md (Pattern 3, Pitfall 4)
    - .planning/phases/10-hybrid-liff-free-messaging/10-02-SUMMARY.md
  </read_first>
  <action>
1. **Create shared helper file `apps/web/lib/liff-messaging.ts`**:
   ```typescript
   import type { Liff } from "@line/liff";

   export function getLiffContextHeader(liff: Liff | null): Record&lt;string, string&gt; {
     const context = liff?.getContext();
     const isInChat = ["group", "room", "utou"].includes(context?.type ?? "");
     return isInChat ? { "X-Liff-Context": "in-line" } : { "X-Liff-Context": "external" };
   }

   export async function trySendMessages(liff: Liff | null, flexCard: unknown): Promise&lt;void&gt; {
     if (!liff || !flexCard) return;
     const context = liff.getContext();
     const isInChat = ["group", "room", "utou"].includes(context?.type ?? "");
     if (!isInChat) return;
     try {
       await liff.sendMessages([flexCard as any]);
     } catch (err) {
       console.error("sendMessages failed:", err);
     }
   }
   ```

2. **Modify the register handler** in the event page (the fetch call to POST /api/proxy/registrations):
   - Import `getLiffContextHeader` and `trySendMessages` from `@/lib/liff-messaging`.
   - Get `{ liff }` from `useLiff()` (already available).
   - Add `...getLiffContextHeader(liff)` to the fetch headers.
   - After successful 201 response, parse `data.flexCard` from response.
   - Call `await trySendMessages(liff, data.flexCard)`.
   - Keep existing `toast.success("ลงทะเบียนสำเร็จ")` and `fetchData()` calls.

3. **Modify the cancel handler** (DELETE /api/proxy/registrations/:id):
   - Same pattern: add X-Liff-Context header, parse flexCard from response, call trySendMessages.

4. **Modify admin remove handler** if present on this page — same pattern.

5. **Do NOT change** the data fetching (GET) calls — those don't send cards.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "sendMessages" "app/(liff)/events/[id]/page.tsx" && grep -q "X-Liff-Context" "app/(liff)/events/[id]/page.tsx" && grep -q "flexCard" "app/(liff)/events/[id]/page.tsx" && test -f lib/liff-messaging.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - lib/liff-messaging.ts exists and exports `getLiffContextHeader` and `trySendMessages`
    - Event page imports from `@/lib/liff-messaging`
    - Event page contains `liff.sendMessages` or `trySendMessages` call
    - Event page contains `X-Liff-Context` header in fetch calls for register and cancel
    - Event page contains `flexCard` parsing from API response
    - Event page contains `console.error("sendMessages failed"` for silent failure
  </acceptance_criteria>
  <done>Register and cancel actions use sendMessages inside LINE. External browser sends header for server pushMessage fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Add sendMessages to event creation and template pages</name>
  <files>apps/web/app/(liff)/events/create/page.tsx, apps/web/app/(liff)/events/templates/[id]/edit/page.tsx</files>
  <read_first>
    - apps/web/app/(liff)/events/create/page.tsx
    - apps/web/app/(liff)/events/templates/[id]/edit/page.tsx
    - apps/web/lib/liff-messaging.ts
    - apps/web/components/liff/liff-provider.tsx
  </read_first>
  <action>
1. **In `apps/web/app/(liff)/events/create/page.tsx`**:
   - Import `getLiffContextHeader` and `trySendMessages` from `@/lib/liff-messaging`.
   - Find the event creation submit handler (POST to /api/proxy/events).
   - Add `...getLiffContextHeader(liff)` to the fetch headers.
   - After successful 201 response, parse `data.flexCard`.
   - Call `await trySendMessages(liff, data.flexCard)`.
   - Per D-06: admin creating event in LINE sends the card as their own message.

2. **In `apps/web/app/(liff)/events/templates/[id]/edit/page.tsx`**:
   - If this page has a "create now" action that POSTs to create an event from template, apply same pattern.
   - Import from `@/lib/liff-messaging`, add X-Liff-Context header and sendMessages after success.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "sendMessages\|trySendMessages" "app/(liff)/events/create/page.tsx" && grep -q "X-Liff-Context\|getLiffContextHeader" "app/(liff)/events/create/page.tsx" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - events/create/page.tsx imports from `@/lib/liff-messaging`
    - events/create/page.tsx contains trySendMessages call after event creation
    - events/create/page.tsx sends X-Liff-Context header on event creation fetch
    - templates/[id]/edit/page.tsx has sendMessages integration if it has create-now action
  </acceptance_criteria>
  <done>Event creation and template actions use sendMessages inside LINE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify sendMessages and external browser flow</name>
  <action>Human verification of the complete hybrid LIFF implementation across all three plans.</action>
  <what-built>
    Full hybrid LIFF implementation:
    - LiffProvider supports external browser login via liff.login()
    - All pages moved to clean URLs (/events/[id], /setup, /profile, etc.)
    - Old /liff/* URLs 301-redirect to new paths
    - Register, cancel, create event use liff.sendMessages() inside LINE
    - External browser falls back to server pushMessage
  </what-built>
  <how-to-verify>
    1. **301 redirects:** Open browser to `http://localhost:3000/liff/events/some-id` — should redirect to `/events/some-id`
    2. **External browser login:** Open `http://localhost:3000/events/{real-event-id}` in Chrome — should redirect to LINE Login, then return with full functionality
    3. **In-LINE sendMessages:** Open a Flex card link in LINE app, register for an event — new card should appear in the group chat as YOUR message (not bot's)
    4. **Cancel sendMessages:** Cancel registration in LINE — updated card should appear in group
    5. **External browser fallback:** Register from Chrome browser — card should appear in group as BOT message (pushMessage fallback)
    6. **Verify `chat_message.write` scope** is enabled in LINE Developers Console for the LIFF app

    **Pre-requisite:** Ensure `chat_message.write` scope is enabled on the LIFF app in LINE Developers Console before testing step 3.
  </how-to-verify>
  <verify>Human confirms all 6 verification steps pass</verify>
  <done>All hybrid LIFF flows verified: external browser login, sendMessages in LINE, pushMessage fallback, 301 redirects.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → liff.sendMessages | Client sends Flex card JSON into LINE chat |
| API flexCard response → client | API-built card JSON consumed by client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | flexCard JSON manipulation | accept | Client receives card JSON from trusted API. Even if client modified it before sendMessages, it only affects what appears in their own chat — no server-side impact. |
| T-10-07 | Repudiation | Cards sent as user message | accept | Per D-05 — cards appear as user's message, not bot. This is the expected behavior. No repudiation concern since registration is tracked server-side regardless. |
</threat_model>

<verification>
- Register in LINE → card appears in group as user message
- Register in Chrome → card appears in group as bot message
- Cancel in LINE → updated card appears as user message
- /liff/events/:id redirects to /events/:id with 301
</verification>

<success_criteria>
- All user-initiated actions in LINE use sendMessages (free)
- External browser actions fall back to pushMessage (paid, correct)
- Cron-generated events still use pushMessage
- No sendMessages errors for in-chat context
</success_criteria>

<output>
After completion, create `.planning/phases/10-hybrid-liff-free-messaging/10-03-SUMMARY.md`
</output>
