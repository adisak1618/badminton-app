---
phase: 10-hybrid-liff-free-messaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/registrations.ts
  - apps/api/src/routes/events.ts
  - apps/api/src/routes/event-templates.ts
  - apps/api/src/lib/repost-card.ts
  - apps/api/src/lib/flex-messages.ts
autonomous: true
requirements:
  - SC-2
  - SC-3

must_haves:
  truths:
    - "Registration POST returns flexCard JSON in response body"
    - "Registration DELETE returns flexCard JSON in response body"
    - "Server skips pushMessage for user-initiated actions when X-Liff-Context header is not 'external'"
    - "Server calls pushMessage only when X-Liff-Context is 'external' or for cron"
    - "Event creation POST returns flexCard JSON and skips pushMessage when in-LINE"
  artifacts:
    - path: "apps/api/src/routes/registrations.ts"
      provides: "Registration routes returning flexCard + conditional pushMessage"
      contains: "flexCard"
    - path: "apps/api/src/lib/repost-card.ts"
      provides: "buildFlexCardData function (build without push)"
      contains: "buildFlexCardData"
  key_links:
    - from: "apps/api/src/routes/registrations.ts"
      to: "apps/api/src/lib/repost-card.ts"
      via: "buildFlexCardData call"
      pattern: "buildFlexCardData"
---

<objective>
Modify API routes to return Flex card JSON in responses and conditionally skip server-side pushMessage for user-initiated actions. Server pushMessage only fires for cron or external browser fallback.

Purpose: Per D-04/D-07 — enable client-side sendMessages by providing the card JSON, and stop wasting Messaging API quota on user-initiated actions inside LINE.
Output: Registration and event routes return flexCard in response; repost-card.ts has a build-only function.
</objective>

<execution_context>
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-liff-free-messaging/10-CONTEXT.md
@.planning/phases/10-hybrid-liff-free-messaging/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildFlexCardData to repost-card.ts and return flexCard from registration routes</name>
  <files>apps/api/src/lib/repost-card.ts, apps/api/src/routes/registrations.ts</files>
  <read_first>
    - apps/api/src/lib/repost-card.ts
    - apps/api/src/lib/flex-messages.ts
    - apps/api/src/routes/registrations.ts
    - .planning/phases/10-hybrid-liff-free-messaging/10-RESEARCH.md (Pattern 2, "Determining Whether Server Should pushMessage")
  </read_first>
  <action>
1. **In `apps/api/src/lib/repost-card.ts`**, extract card-building logic into a new exported function `buildFlexCardData`:
   ```typescript
   export async function buildFlexCardData(opts: {
     event: typeof events.$inferSelect;
     clubId: string;
     action: "register" | "cancel" | "admin_remove" | "close" | "reopen";
     memberName?: string;
     registeredCount: number;
   }): Promise<{ card: FlexMessage; altText: string; club: { lineGroupId: string } } | null> {
   ```
   - Move lines 14-72 (club fetch, LIFF URL build, altText build, card build) into this function.
   - Return `{ card, altText, club: { lineGroupId: club.lineGroupId } }` or `null` if no club/lineGroupId.
   - Refactor existing `repostFlexCard` to call `buildFlexCardData` then do the pushMessage (lines 74-89). This avoids duplication.

2. **In `apps/api/src/routes/registrations.ts`**, modify the POST handler:
   - Read the `X-Liff-Context` header: `const liffContext = request.headers.get("x-liff-context");`
   - After computing `newCount`, call `buildFlexCardData(...)` to get the card JSON.
   - If `liffContext === "external"` or `liffContext` is null/absent: call `repostFlexCard(...)` as before (server pushes to group). Set `flexCard: null` in response.
   - If `liffContext` is any other value (meaning client is in LINE): skip `repostFlexCard`, include `flexCard: cardData.card` in response.
   - Update the 201 response shape: `{ id: regId, registeredCount: Number(newCount), flexCard: cardData?.card ?? null }`

3. **In `apps/api/src/routes/registrations.ts`**, modify the DELETE handler similarly:
   - Read `X-Liff-Context` header.
   - Call `buildFlexCardData(...)` for card JSON.
   - If external/absent header → call `repostFlexCard(...)`, return `flexCard: null`.
   - If in-LINE → skip push, return `flexCard: cardData.card`.
   - Update response: `{ registeredCount: Number(newCount), flexCard: cardData?.card ?? null }`

4. **Also update the close/reopen endpoints** in registrations.ts if they call `repostFlexCard` — apply same pattern. (Check if these exist — they may be in events.ts instead.)
  </action>
  <verify>
    <automated>cd apps/api && grep -q "buildFlexCardData" src/lib/repost-card.ts && grep -q "flexCard" src/routes/registrations.ts && grep -q "x-liff-context" src/routes/registrations.ts && bun test --bail && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - repost-card.ts exports `buildFlexCardData` function
    - repost-card.ts `repostFlexCard` still exists and calls `buildFlexCardData` internally
    - registrations.ts POST handler reads `x-liff-context` header
    - registrations.ts POST 201 response includes `flexCard` field
    - registrations.ts DELETE handler reads `x-liff-context` header
    - registrations.ts DELETE response includes `flexCard` field
    - `bun test` passes in apps/api
  </acceptance_criteria>
  <done>Registration routes return Flex card JSON for client-side sendMessages. Server pushMessage only fires for external browser or absent header.</done>
</task>

<task type="auto">
  <name>Task 2: Return flexCard from event creation and template routes, skip pushMessage when in-LINE</name>
  <files>apps/api/src/routes/events.ts, apps/api/src/routes/event-templates.ts</files>
  <read_first>
    - apps/api/src/routes/events.ts
    - apps/api/src/routes/event-templates.ts
    - apps/api/src/lib/flex-messages.ts
    - apps/api/src/routes/cron.ts (to confirm cron stays unchanged)
  </read_first>
  <action>
1. **In `apps/api/src/routes/events.ts`** — POST event creation handler (around line 116):
   - Read `X-Liff-Context` header: `const liffContext = request.headers.get("x-liff-context");`
   - The current code builds `flexCard` (line 100) then calls `lineClient.pushMessage` (line 116).
   - If `liffContext !== "external"` and `liffContext` is not null: skip the `pushMessage` call. Return the built `flexCard` in the response: add `flexCard` field to the 201 response.
   - If `liffContext === "external"` or null: keep pushMessage as-is, return `flexCard: null`.
   - Per D-06: admin creating event in LINE uses sendMessages (client side). Cron stays unchanged.
   - Still store `lineMessageId` when pushMessage is called. When skipped, set `lineMessageId` to null (card sent by user via sendMessages doesn't give us a message ID we can track).

2. **In `apps/api/src/routes/event-templates.ts`** — "create-now" handler (around line 266):
   - Same pattern: read `X-Liff-Context` header, conditionally skip pushMessage, return flexCard in response.
   - The cancel push at line 339 — this is an admin action that could also use sendMessages. Apply same header-based conditional.

3. **Do NOT modify `apps/api/src/routes/cron.ts`** — cron has no user context, always uses pushMessage. Per D-07, this is correct.
  </action>
  <verify>
    <automated>cd apps/api && grep -q "x-liff-context" src/routes/events.ts && grep -q "flexCard" src/routes/events.ts && grep -q "x-liff-context" src/routes/event-templates.ts && bun test --bail && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - events.ts POST handler reads `x-liff-context` header
    - events.ts POST response includes `flexCard` field
    - events.ts skips `lineClient.pushMessage` when header is not "external" and not absent
    - event-templates.ts create-now handler reads `x-liff-context` header and conditionally skips pushMessage
    - cron.ts is NOT modified (still uses pushMessage unconditionally)
    - `bun test` passes in apps/api
  </acceptance_criteria>
  <done>Event creation and template routes return Flex card JSON for client-side sendMessages. Cron unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | X-Liff-Context header is client-supplied, untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Spoofing | X-Liff-Context header | accept | Worst case: attacker sends "external" to force pushMessage cost; bounded by LINE rate limits and existing auth. Sending non-external skips push — no security impact, just means group doesn't get card (attacker only hurts themselves). |
| T-10-05 | Information Disclosure | flexCard in response | accept | Flex card contains public event info (title, date, venue, count) — no sensitive data. Already visible in LINE group. |
</threat_model>

<verification>
- `cd apps/api && bun test` all tests pass
- Registration routes return `flexCard` field in responses
- Event creation route conditionally skips pushMessage
- Cron route unchanged
</verification>

<success_criteria>
- API responses include flexCard JSON for client-side sendMessages
- pushMessage only called for cron and external browser fallback
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-hybrid-liff-free-messaging/10-02-SUMMARY.md`
</output>
