---
phase: 10-hybrid-liff-free-messaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/liff/liff-provider.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/(liff)/layout.tsx
  - apps/web/app/(liff)/events/[id]/page.tsx
  - apps/web/app/(liff)/events/create/page.tsx
  - apps/web/app/(liff)/events/templates/page.tsx
  - apps/web/app/(liff)/events/templates/[id]/edit/page.tsx
  - apps/web/app/(liff)/setup/page.tsx
  - apps/web/app/(liff)/profile/page.tsx
  - apps/web/next.config.js
autonomous: true
requirements:
  - SC-1
  - SC-4

must_haves:
  truths:
    - "LiffProvider calls liff.login() in external browser when not logged in"
    - "LiffProvider does not infinite-loop after OAuth return"
    - "All LIFF pages exist under (liff) route group without Nav"
    - "Old /liff/* URLs 301-redirect to new clean paths"
    - "LiffProvider wraps all pages from root layout"
  artifacts:
    - path: "apps/web/components/liff/liff-provider.tsx"
      provides: "External browser login + context detection"
      contains: "liffInstance.login"
    - path: "apps/web/app/(liff)/layout.tsx"
      provides: "LIFF route group layout without Nav"
    - path: "apps/web/app/(liff)/events/[id]/page.tsx"
      provides: "Event details page at new path"
    - path: "apps/web/next.config.js"
      provides: "301 redirects from /liff/* to /*"
      contains: "/liff/events/:id"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "LiffProvider"
      via: "wraps children"
      pattern: "LiffProvider"
    - from: "apps/web/next.config.js"
      to: "/events/:id"
      via: "redirects config"
      pattern: "permanent.*true"
---

<objective>
Restructure LIFF pages: move from /liff/* to clean paths using a (liff) route group, add external browser login support to LiffProvider, move LiffProvider to root layout, and set up 301 redirects.

Purpose: Per D-01/D-08/D-09/D-11 — enable external browser access and clean URLs while maintaining backward compatibility with old Flex card links.
Output: All LIFF pages at new paths, LiffProvider with external browser support, 301 redirects configured.
</objective>

<execution_context>
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hybrid-liff-free-messaging/10-CONTEXT.md
@.planning/phases/10-hybrid-liff-free-messaging/10-RESEARCH.md
@.planning/phases/10-hybrid-liff-free-messaging/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor LiffProvider for external browser + move to root layout</name>
  <files>apps/web/components/liff/liff-provider.tsx, apps/web/app/layout.tsx, apps/web/app/(liff)/layout.tsx</files>
  <read_first>
    - apps/web/components/liff/liff-provider.tsx
    - apps/web/app/layout.tsx
    - apps/web/app/liff/layout.tsx
    - apps/web/lib/env.ts
    - .planning/phases/10-hybrid-liff-free-messaging/10-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 1, Pitfall 3)
  </read_first>
  <action>
1. **Modify `apps/web/components/liff/liff-provider.tsx`** (per D-01, D-02):
   - After `liffInstance.init({ liffId })` resolves, add external browser detection:
     ```
     if (!liffInstance.isInClient() && !liffInstance.isLoggedIn()) {
       liffInstance.login({ redirectUri: window.location.href });
       return; // redirect imminent
     }
     ```
   - This replaces the current lines 71-76 that show "Please ensure this page is opened inside LINE" error. Instead of erroring, we login.
   - Keep the existing `getIDToken()` flow after the guard — it works for both in-LINE (already has token) and post-OAuth-return (isLoggedIn true, has token).
   - Update the setup redirect path from `/liff/setup` to `/setup` (line 58 currently checks `startsWith("/liff/setup")` — change to `startsWith("/setup")`).
   - Update the redirect URL from `/liff/setup?returnTo=...` to `/setup?returnTo=...`.
   - Add `isInClient` to the context value: `isInClient: boolean` — expose whether we are inside LINE. Initialize with `useState<boolean>(false)`, set after init via `setIsInClient(liffInstance.isInClient())`. This is needed by Plan 03 for sendMessages decisions.
   - Export the updated `LiffContextValue` interface with the new `isInClient` field.

2. **Modify `apps/web/app/layout.tsx`** (per D-11):
   - Import `LiffProvider` from `@/components/liff/liff-provider` and `env` from `@/lib/env`.
   - Wrap `{children}` with `<LiffProvider liffId={env.NEXT_PUBLIC_LIFF_ID}>`.
   - Keep `<Nav />`, `<Toaster />`, and existing structure. LiffProvider wraps everything.
   - Result:
     ```tsx
     <body className="min-h-screen bg-background antialiased">
       <LiffProvider liffId={env.NEXT_PUBLIC_LIFF_ID}>
         <Nav />
         <main className="mx-auto max-w-5xl px-4 py-8">{children}</main>
         <Toaster />
       </LiffProvider>
     </body>
     ```

3. **Create `apps/web/app/(liff)/layout.tsx`** — route group layout for LIFF pages (no Nav):
   ```tsx
   export default function LiffGroupLayout({ children }: { children: React.ReactNode }) {
     return (
       <div className="min-h-screen bg-background">
         {children}
       </div>
     );
   }
   ```
   This overrides the root layout's `<Nav />` + `<main>` wrapper. The `(liff)` route group doesn't affect the URL path. LiffProvider is already in root layout so it's available here.

   **Important:** Since Next.js route groups with their own layout replace the parent's `<main>` wrapper, this layout provides `min-h-screen bg-background` matching the old `/liff/layout.tsx` style, but WITHOUT `<Nav />`.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "liffInstance.login" components/liff/liff-provider.tsx && grep -q "isInClient" components/liff/liff-provider.tsx && grep -q "LiffProvider" app/layout.tsx && test -f "app/(liff)/layout.tsx" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - liff-provider.tsx contains `liffInstance.login({ redirectUri: window.location.href })`
    - liff-provider.tsx contains `!liffInstance.isInClient() && !liffInstance.isLoggedIn()` guard
    - liff-provider.tsx does NOT contain `"Please ensure this page is opened inside LINE"`
    - liff-provider.tsx contains `isInClient` in the context value and LiffContextValue interface
    - liff-provider.tsx setup redirect uses `/setup?returnTo=` not `/liff/setup?returnTo=`
    - app/layout.tsx imports and wraps children with `LiffProvider`
    - app/(liff)/layout.tsx exists and does NOT contain `Nav`
  </acceptance_criteria>
  <done>LiffProvider supports external browser login, exposes isInClient, lives in root layout. LIFF route group layout created without Nav.</done>
</task>

<task type="auto">
  <name>Task 2: Move LIFF pages to (liff) route group + configure 301 redirects</name>
  <files>apps/web/app/(liff)/events/[id]/page.tsx, apps/web/app/(liff)/events/create/page.tsx, apps/web/app/(liff)/events/templates/page.tsx, apps/web/app/(liff)/events/templates/[id]/edit/page.tsx, apps/web/app/(liff)/setup/page.tsx, apps/web/app/(liff)/profile/page.tsx, apps/web/next.config.js</files>
  <read_first>
    - apps/web/app/liff/events/[id]/page.tsx
    - apps/web/app/liff/events/create/page.tsx
    - apps/web/app/liff/events/templates/page.tsx
    - apps/web/app/liff/events/templates/[id]/edit/page.tsx
    - apps/web/app/liff/setup/page.tsx
    - apps/web/app/liff/profile/page.tsx
    - apps/web/app/liff/page.tsx
    - apps/web/next.config.js
  </read_first>
  <action>
1. **Move all LIFF page files** from `apps/web/app/liff/` to `apps/web/app/(liff)/`:
   - `app/liff/events/[id]/page.tsx` → `app/(liff)/events/[id]/page.tsx`
   - `app/liff/events/create/page.tsx` → `app/(liff)/events/create/page.tsx`
   - `app/liff/events/templates/page.tsx` → `app/(liff)/events/templates/page.tsx`
   - `app/liff/events/templates/[id]/edit/page.tsx` → `app/(liff)/events/templates/[id]/edit/page.tsx`
   - `app/liff/setup/page.tsx` → `app/(liff)/setup/page.tsx`
   - `app/liff/profile/page.tsx` → `app/(liff)/profile/page.tsx`
   - If `app/liff/page.tsx` exists (index), move it to `app/(liff)/page.tsx` or remove if unnecessary.

2. **No import path changes needed** — the files use `@/` aliases which resolve from `apps/web/` root regardless of file location. Verify: imports like `@/components/liff/liff-provider`, `@/lib/validations/event`, `@repo/ui/components/button` all use absolute aliases.

3. **Delete the old `apps/web/app/liff/` directory** after moving all files. The `liff/layout.tsx` is no longer needed — LiffProvider is in root layout and `(liff)/layout.tsx` handles the no-Nav wrapper.

4. **Update `apps/web/next.config.js` redirects** (per D-09):
   Replace the existing redirects array with:
   ```js
   async redirects() {
     return [
       { source: "/liff/events/:id/register", destination: "/events/:id", permanent: true },
       { source: "/liff/events/:id", destination: "/events/:id", permanent: true },
       { source: "/liff/events/create", destination: "/events/create", permanent: true },
       { source: "/liff/events/templates", destination: "/events/templates", permanent: true },
       { source: "/liff/events/templates/:id/edit", destination: "/events/templates/:id/edit", permanent: true },
       { source: "/liff/setup", destination: "/setup", permanent: true },
       { source: "/liff/profile", destination: "/profile", permanent: true },
     ];
   }
   ```

5. **Verify the build compiles** — run `cd apps/web && bunx next build` or at minimum `bunx next lint` to catch broken imports.
  </action>
  <verify>
    <automated>cd apps/web && test -f "app/(liff)/events/[id]/page.tsx" && test -f "app/(liff)/setup/page.tsx" && test -f "app/(liff)/profile/page.tsx" && test ! -d "app/liff" && grep -c "permanent.*true" next.config.js | grep -q "7" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(liff)/events/[id]/page.tsx` exists
    - `app/(liff)/events/create/page.tsx` exists
    - `app/(liff)/events/templates/page.tsx` exists
    - `app/(liff)/events/templates/[id]/edit/page.tsx` exists
    - `app/(liff)/setup/page.tsx` exists
    - `app/(liff)/profile/page.tsx` exists
    - `app/liff/` directory does NOT exist (fully removed)
    - `next.config.js` contains 7 redirect entries with `permanent: true`
    - `next.config.js` contains `source: "/liff/events/:id"` redirecting to `destination: "/events/:id"`
    - `next.config.js` contains `source: "/liff/setup"` redirecting to `destination: "/setup"`
  </acceptance_criteria>
  <done>All LIFF pages live at clean URL paths via (liff) route group. Old /liff/* paths 301-redirect. Old liff/ directory removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External browser → LIFF OAuth | Untrusted browser initiates LINE Login |
| returnTo parameter | User-controlled redirect target after profile setup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | liff.login() redirectUri | accept | LINE server validates redirectUri matches LIFF Endpoint URL pattern — enforced server-side by LINE platform |
| T-10-02 | Tampering | returnTo query param in /setup redirect | mitigate | Validate returnTo is a relative path starting with "/" and does not contain "://" — add check in liff-provider.tsx before redirect |
| T-10-03 | Denial of Service | liff.login() infinite loop | mitigate | Guard with `!isInClient() && !isLoggedIn()` per RESEARCH.md Pitfall 1 |
</threat_model>

<verification>
- `cd apps/web && bunx next build` completes without errors
- All 7 redirect entries in next.config.js
- LiffProvider contains external browser login guard
- No `/liff/` directory remains
</verification>

<success_criteria>
- LIFF pages accessible at /events/[id], /setup, /profile, /events/create, /events/templates, /events/templates/[id]/edit
- Old /liff/* URLs return 301 to new paths
- LiffProvider wraps all pages from root layout
- External browser users trigger LINE Login OAuth flow
</success_criteria>

<output>
After completion, create `.planning/phases/10-hybrid-liff-free-messaging/10-01-SUMMARY.md`
</output>
