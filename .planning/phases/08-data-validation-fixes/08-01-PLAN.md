---
phase: 08-data-validation-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/events.ts
  - apps/web/app/liff/layout.tsx
autonomous: true
requirements:
  - EVNT-01
  - BOT-01
must_haves:
  truths:
    - "venueName longer than 255 chars is rejected by API validation"
    - "LIFF layout reads LIFF_ID from validated env module, not raw process.env"
  artifacts:
    - path: "apps/api/src/routes/events.ts"
      provides: "venueName maxLength aligned to DB varchar(255)"
      contains: "maxLength: 255"
    - path: "apps/web/app/liff/layout.tsx"
      provides: "LIFF_ID from validated env"
      contains: 'env.NEXT_PUBLIC_LIFF_ID'
  key_links:
    - from: "apps/api/src/routes/events.ts"
      to: "packages/db/src/schema/events.ts"
      via: "maxLength matches varchar length"
      pattern: "maxLength: 255"
    - from: "apps/web/app/liff/layout.tsx"
      to: "apps/web/lib/env.ts"
      via: "import { env }"
      pattern: 'from "@/lib/env"'
---

<objective>
Fix two integration gaps from v1.0 milestone audit: align venueName maxLength between API validation and DB column, and replace raw process.env access for LIFF_ID with the validated env module.

Purpose: Prevent DB truncation errors from oversized venueName input and ensure LIFF_ID is validated at startup rather than silently empty.
Output: Two corrected source files with consistent validation.
</objective>

<execution_context>
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-validation-fixes/08-CONTEXT.md
@.planning/phases/08-data-validation-fixes/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shrink venueName maxLength from 500 to 255 in API validation (per D-01)</name>
  <files>apps/api/src/routes/events.ts</files>
  <read_first>
    - apps/api/src/routes/events.ts
    - packages/db/src/schema/events.ts
  </read_first>
  <action>
In `apps/api/src/routes/events.ts`, find the TypeBox schema for event creation body (around line 148). Change `maxLength: 500` to `maxLength: 255` on the `venueName` field. Leave `minLength: 1` intact. Do not change any other fields.

Before: `venueName: t.String({ minLength: 1, maxLength: 500 })`
After: `venueName: t.String({ minLength: 1, maxLength: 255 })`
  </action>
  <verify>
    <automated>grep -n "maxLength: 255" apps/api/src/routes/events.ts | grep -i venue</automated>
  </verify>
  <acceptance_criteria>
    - `grep "maxLength: 500" apps/api/src/routes/events.ts` returns NO matches
    - `grep "maxLength: 255" apps/api/src/routes/events.ts` returns at least one match on the venueName line
    - `grep "minLength: 1" apps/api/src/routes/events.ts` still returns a match for venueName (not accidentally removed)
  </acceptance_criteria>
  <done>venueName maxLength is 255 in the API TypeBox schema, matching the DB varchar(255) column</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw process.env with validated env for LIFF_ID (per D-02)</name>
  <files>apps/web/app/liff/layout.tsx</files>
  <read_first>
    - apps/web/app/liff/layout.tsx
    - apps/web/lib/env.ts
  </read_first>
  <action>
In `apps/web/app/liff/layout.tsx`:

1. Add import at the top: `import { env } from "@/lib/env";`
2. Replace `process.env.NEXT_PUBLIC_LIFF_ID || ""` (or similar raw access on ~line 5) with `env.NEXT_PUBLIC_LIFF_ID`
3. Remove the `|| ""` fallback — the validated env guarantees non-empty string
4. Do NOT add `"use client"` directive — the layout is a Server Component and `@t3-oss/env-nextjs` client vars are accessible on the server
  </action>
  <verify>
    <automated>grep -n 'env.NEXT_PUBLIC_LIFF_ID' apps/web/app/liff/layout.tsx && ! grep 'process\.env\.NEXT_PUBLIC_LIFF_ID' apps/web/app/liff/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'from "@/lib/env"' apps/web/app/liff/layout.tsx` returns a match
    - `grep 'env.NEXT_PUBLIC_LIFF_ID' apps/web/app/liff/layout.tsx` returns a match
    - `grep 'process.env.NEXT_PUBLIC_LIFF_ID' apps/web/app/liff/layout.tsx` returns NO matches
    - `grep '"use client"' apps/web/app/liff/layout.tsx` returns NO matches (still a Server Component)
  </acceptance_criteria>
  <done>LIFF layout reads LIFF_ID from validated env module; raw process.env access is removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Untrusted input (venueName) crosses into API validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | events.ts venueName | mitigate | Set TypeBox maxLength: 255 to match DB varchar(255) — prevents DB truncation/error from oversized input |
</threat_model>

<verification>
1. `grep "maxLength: 255" apps/api/src/routes/events.ts` shows venueName constraint
2. `grep 'from "@/lib/env"' apps/web/app/liff/layout.tsx` shows validated env import
3. `grep 'process.env.NEXT_PUBLIC_LIFF_ID' apps/web/app/liff/layout.tsx` returns nothing
</verification>

<success_criteria>
- venueName API validation maxLength matches DB varchar(255) — no valid API input can exceed DB column
- LIFF layout uses validated env module for LIFF_ID — missing env var causes startup error, not silent empty string
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-validation-fixes/08-01-SUMMARY.md`
</output>
