---
phase: 09-event-details-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/liff/events/[id]/page.tsx
  - apps/web/app/liff/events/[id]/register/page.tsx
  - apps/web/next.config.js
autonomous: true
requirements:
  - BOT-01
must_haves:
  truths:
    - "Visiting /liff/events/[id] renders the event details and registration page"
    - "Visiting /liff/events/[id]/register redirects 301 to /liff/events/[id]"
    - "Cancelled events show a 'ยกเลิกแล้ว' badge and disabled button"
  artifacts:
    - path: "apps/web/app/liff/events/[id]/page.tsx"
      provides: "Unified event details + registration page"
      min_lines: 200
    - path: "apps/web/next.config.js"
      provides: "301 redirect from /register to parent path"
      contains: "redirects"
  key_links:
    - from: "apps/web/next.config.js"
      to: "/liff/events/:id/register"
      via: "Next.js redirects config"
      pattern: "liff/events/:id/register"
---

<objective>
Move the existing register page to `/liff/events/[id]/page.tsx`, add a 301 redirect for the old `/register` path, and add cancelled event state handling.

Purpose: Closes the Details CTA 404 gap — the unified page serves both "Register" and "Details" taps from Flex Message cards.
Output: Working event details page at `/liff/events/[id]`, old URL redirects, cancelled state badge.
</objective>

<execution_context>
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-event-details-page/09-CONTEXT.md
@.planning/phases/09-event-details-page/09-UI-SPEC.md

<interfaces>
<!-- Current register page — will be moved as-is with additions -->
From apps/web/app/liff/events/[id]/register/page.tsx:
- Default export: LiffEventRegisterPage (wraps LiffEventRegisterInner in Suspense)
- EventData interface: { id, title, eventDate, venueName, venueMapsUrl, status: "open"|"closed"|"cancelled", maxPlayers, clubId }
- Existing states: isFull, isClosed, isRegistered
- Missing state: isCancelled (event.status === "cancelled")

From apps/web/next.config.js:
- Currently bare: `const nextConfig = {}; export default nextConfig;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move register page to parent route and add cancelled state</name>
  <files>apps/web/app/liff/events/[id]/page.tsx, apps/web/app/liff/events/[id]/register/page.tsx</files>
  <read_first>
    - apps/web/app/liff/events/[id]/register/page.tsx
    - .planning/phases/09-event-details-page/09-UI-SPEC.md
  </read_first>
  <action>
1. Copy `apps/web/app/liff/events/[id]/register/page.tsx` to `apps/web/app/liff/events/[id]/page.tsx`.
2. Delete the original `apps/web/app/liff/events/[id]/register/page.tsx` (and remove the `register/` directory if empty).
3. In the new `page.tsx`, add cancelled state handling per D-08:
   - Add `const isCancelled = event?.status === "cancelled";` after the existing `isClosed` line (~line 108).
   - Add cancelled badge after the full-state badge block (after line ~214):
     ```tsx
     {isCancelled && (
       <span className="inline-block mb-4 bg-destructive text-destructive-foreground px-2 py-1 rounded text-sm">
         ยกเลิกแล้ว
       </span>
     )}
     ```
   - Update Button disabled prop from:
     `disabled={submitting || (isFull && !isRegistered) || isClosed}`
     to:
     `disabled={submitting || (isFull && !isRegistered) || isClosed || isCancelled}`
   - Update button label ternary. Insert `isCancelled` branch before the `isClosed` branch:
     ```tsx
     ) : isCancelled ? (
       "ยกเลิกแล้ว"
     ) : isClosed ? (
     ```
   - Also add venue maps link display. After the venueName paragraph, add:
     ```tsx
     {event.venueMapsUrl && (
       <a href={event.venueMapsUrl} target="_blank" rel="noopener noreferrer" className="text-sm text-primary underline">
         Google Maps
       </a>
     )}
     ```
  </action>
  <verify>
    <automated>ls apps/web/app/liff/events/\[id\]/page.tsx && ! ls apps/web/app/liff/events/\[id\]/register/page.tsx 2>/dev/null && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/liff/events/[id]/page.tsx` exists and contains `export default function LiffEventRegisterPage`
    - `apps/web/app/liff/events/[id]/register/page.tsx` does NOT exist
    - `page.tsx` contains `const isCancelled = event?.status === "cancelled"`
    - `page.tsx` contains `ยกเลิกแล้ว` (cancelled badge text)
    - `page.tsx` contains `|| isCancelled` in the disabled prop
    - `page.tsx` contains `event.venueMapsUrl` link
  </acceptance_criteria>
  <done>Unified page exists at /liff/events/[id], old register directory removed, cancelled state renders badge and disables button</done>
</task>

<task type="auto">
  <name>Task 2: Add 301 redirect for old /register path</name>
  <files>apps/web/next.config.js</files>
  <read_first>
    - apps/web/next.config.js
  </read_first>
  <action>
Update `apps/web/next.config.js` to add a redirects function per D-02. Replace the entire file with:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  async redirects() {
    return [
      {
        source: "/liff/events/:id/register",
        destination: "/liff/events/:id",
        permanent: true,
      },
    ];
  },
};

export default nextConfig;
```

Use `:id` colon syntax (not `[id]`) for the dynamic segment — this is Next.js redirect config syntax.
  </action>
  <verify>
    <automated>grep -q "liff/events/:id/register" apps/web/next.config.js && grep -q "permanent: true" apps/web/next.config.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/next.config.js` contains `source: "/liff/events/:id/register"`
    - `apps/web/next.config.js` contains `destination: "/liff/events/:id"`
    - `apps/web/next.config.js` contains `permanent: true`
    - `apps/web/next.config.js` contains `async redirects()`
  </acceptance_criteria>
  <done>Old /liff/events/[id]/register URLs return 301 redirect to /liff/events/[id]</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LIFF page | No new trust boundaries — reuses existing LIFF auth from Phase 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | /liff/events/[id] page | accept | No new auth surface; existing useLiff hook verifies identity |
| T-09-02 | Information Disclosure | Event details page | accept | Event data already accessible via existing registration page; no change in access model |
</threat_model>

<verification>
- `apps/web/app/liff/events/[id]/page.tsx` exists with cancelled state handling
- `apps/web/app/liff/events/[id]/register/` directory removed
- `apps/web/next.config.js` has redirect from `/liff/events/:id/register` to `/liff/events/:id`
- `npx next build` compiles without errors (if build is available)
</verification>

<success_criteria>
- Page renders at /liff/events/[id] showing event info, registration list, register/cancel button
- Cancelled events show badge and disabled button
- Old /register URLs redirect with 301
</success_criteria>

<output>
After completion, create `.planning/phases/09-event-details-page/09-01-SUMMARY.md`
</output>
