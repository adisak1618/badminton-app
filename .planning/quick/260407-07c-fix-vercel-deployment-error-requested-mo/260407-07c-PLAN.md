---
phase: 260407-07c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/index.ts
  - apps/api/vercel.json
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "Vercel deployment succeeds without 'Requested module is not instantiated yet' error"
    - "GET /health returns { status: 'ok' } on deployed Vercel instance"
    - "LINE webhook endpoint remains functional with signature verification"
  artifacts:
    - path: "apps/api/src/index.ts"
      provides: "Elysia app with Node.js adapter"
      contains: "adapter: node()"
    - path: "apps/api/vercel.json"
      provides: "Vercel config without Bun runtime"
    - path: "apps/api/package.json"
      provides: "@elysiajs/node dependency"
      contains: "@elysiajs/node"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "@elysiajs/node"
      via: "import { node } from '@elysiajs/node'"
      pattern: "adapter:\\s*node\\(\\)"
---

<objective>
Fix Vercel deployment error "Requested module is not instantiated yet" by switching from Bun runtime to Node.js runtime using the official @elysiajs/node adapter.

Purpose: The @line/bot-sdk barrel export (~3400 ESM files) triggers a known Bun module linking bug on Vercel. Switching to Node.js runtime is the community-confirmed fix. Version pinning will NOT help since all @line/bot-sdk versions have similar module structures.

Output: Working Vercel deployment with Node.js runtime, all existing endpoints preserved.
</objective>

<execution_context>
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adisakchaiyakul/project/badminton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/api/src/index.ts
@apps/api/vercel.json
@apps/api/package.json
@apps/api/src/webhook/line.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @elysiajs/node adapter and switch runtime</name>
  <files>apps/api/package.json, apps/api/src/index.ts, apps/api/vercel.json</files>
  <action>
1. Install the Node.js adapter:
   ```
   cd apps/api && bun add @elysiajs/node
   ```

2. Update `apps/api/src/index.ts`:
   - Add import: `import { node } from "@elysiajs/node";`
   - Change `new Elysia()` to `new Elysia({ adapter: node() })`
   - Keep ALL existing routes, plugins, and the default export exactly as-is

3. Update `apps/api/vercel.json`:
   - Remove `"bunVersion": "1.x"` entry. If the file becomes an empty `{}` object, delete the file entirely.

IMPORTANT: Do NOT change any route logic, webhook handler code, or middleware. The only changes are the adapter import, Elysia constructor option, and vercel.json runtime config.
  </action>
  <verify>
    <automated>cd /Users/adisakchaiyakul/project/badminton/apps/api && bun run src/index.ts &amp; sleep 2 &amp;&amp; curl -s http://localhost:3000/health &amp;&amp; kill %1 2>/dev/null; true</automated>
  </verify>
  <done>
- @elysiajs/node is in package.json dependencies
- index.ts uses `adapter: node()` in Elysia constructor
- vercel.json no longer contains "bunVersion": "1.x"
- Local health endpoint responds with { status: "ok" }
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify deployment build succeeds locally</name>
  <files>apps/api/src/index.ts</files>
  <action>
1. Run a dry-run type check to ensure no TypeScript errors from the adapter change:
   ```
   cd apps/api && bunx tsc --noEmit --pretty 2>&1 || true
   ```
   If tsc is not configured, at minimum verify the import resolves:
   ```
   bun run -e "import { node } from '@elysiajs/node'; console.log('adapter ok:', typeof node)"
   ```

2. Verify that the @line/bot-sdk imports still resolve correctly alongside the node adapter:
   ```
   bun run -e "import { validateSignature } from '@line/bot-sdk'; console.log('line sdk ok:', typeof validateSignature)"
   ```

3. If any import fails, check that all dependencies are installed (`bun install` in monorepo root).
  </action>
  <verify>
    <automated>cd /Users/adisakchaiyakul/project/badminton/apps/api && bun run -e "import { node } from '@elysiajs/node'; import { validateSignature } from '@line/bot-sdk'; console.log('all imports ok')"</automated>
  </verify>
  <done>
- @elysiajs/node import resolves without error
- @line/bot-sdk import resolves without error
- No TypeScript compilation errors from the adapter change
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | LINE webhook requests with HMAC signatures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07c-01 | T (Tampering) | webhook signature | accept | No change -- validateSignature from @line/bot-sdk works identically on Node.js adapter. request.text() behavior confirmed compatible. |
</threat_model>

<verification>
1. `@elysiajs/node` present in apps/api/package.json
2. `adapter: node()` in Elysia constructor in apps/api/src/index.ts
3. No `bunVersion` in apps/api/vercel.json (or file deleted)
4. All imports resolve: `@elysiajs/node`, `@line/bot-sdk`, `elysia`
5. Local server starts and /health responds
</verification>

<success_criteria>
- Vercel deployment completes without "Requested module is not instantiated yet" error
- /health endpoint returns 200 with { status: "ok" }
- Webhook endpoint remains functional (same route structure, same signature verification)
</success_criteria>

<output>
After completion, create `.planning/quick/260407-07c-fix-vercel-deployment-error-requested-mo/260407-07c-SUMMARY.md`
</output>
